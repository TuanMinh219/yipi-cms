import { pageConfigs } from '@/assets/data/learningData'
import EntityPage from '@/pages/shared/EntityPage'

export default function Vocabulary() {
  return <EntityPage config={pageConfigs.vocabulary} />
}
