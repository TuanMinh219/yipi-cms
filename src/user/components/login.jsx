import React from 'react';
import './login.css';

export default function Login() {
  return (
    <div className="page-placeholder">
      <h2>Login (converted)</h2>
      <p>Converted from <code>login.html</code>. Use `/admin/login` for admin login.</p>
    </div>
  );
}
